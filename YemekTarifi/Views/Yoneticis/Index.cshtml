@model IEnumerable<YemekTarifi.Models.DB.Yonetici>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sıfre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sıfre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.YoneticiId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.YoneticiId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.YoneticiId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
